---
title: "Bienvenue"
description: "scheint, als hättest du dich auf meinen Blog verirrt. Hier halte ich fest, was Strava irgendwann verschluckt und nicht zeigt: Bikepackingtouren, Träume und Projekte."
output:
  distill::distill_article:
    css: "strapless.css"


---



```{r setup, echo=FALSE}
knitr::opts_chunk$set(echo = FALSE)
source(file.path("R", "functions.R"))
library(distilltools)
library(tidyverse)
```

```{r projects, echo=FALSE, layout="l-body-outset"}
projects <- readr::read_csv2(here::here("content", "overview.csv"))
projects %>% 
  bs4cards::cards(
    title = title,
    text = description,
    link = url,
    image = src,
    label_colour = "#ffffff",
    tags = paste("All;", tags),
    footer = tags,
    border_radius = 5
  )
```